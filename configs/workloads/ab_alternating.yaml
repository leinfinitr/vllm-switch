name: ab_alternating
base_url: http://127.0.0.1:9000
request_rate: 0.5
max_requests: 20
stream: true
pattern:
  type: alternating
  models: [qwen-0.5b, qwen-1.5b]
prompt:
  type: fixed
  text: "Explain what model switching means in one short paragraph."
output:
  max_tokens: 64
